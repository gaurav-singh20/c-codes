//write a programe to find the grocery bills %=
//inputs are product name,id,price,quantity customer name,id
#include <stdio.h>
main()
{
	int n, custid, prodname[n];
	char custname[50],a;
	printf("CUSTOMER NAME: ");
	scanf("%s",&custname);
	printf("CUSTOMER ID: ");
	scanf("%d",&custid);
}
